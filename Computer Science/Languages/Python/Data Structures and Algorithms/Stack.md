```ad-summary 
title: Stack

Operations are performed *LIFO* (last in, first out), which means that the last element added will be the first one removed. A stack can eb implemented using an array or a linked list. If the stack runs out of memory, it's called a *stack overflow*.
```
